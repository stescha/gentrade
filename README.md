# gentrade

A locally‑built [`zigzag`](vendor/dist) wheel is checked in rather than pulling from the original GitHub repository. This is necesary because the original package is not properly mainained. It is not compatible with poetry 2 and has a bug caused by an older cython version. 
